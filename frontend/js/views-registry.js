// Populated by each view module: Views.dashboard = { title, render }, Views.contacts = {...}, etc.
// This file must load BEFORE any file in js/views/ — those files assign into this object.
const Views = {};
